import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';
import './index.css';

// this file is used to render the App.js file to the root element in the index.html file
ReactDOM.render(<App />, document.getElementById('root'));